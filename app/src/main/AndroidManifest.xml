<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="commanderpepper.helpmechoose">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_hmc"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_hmc_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".sortlist.SortListActivity"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".listsdetails.ListsDetailsActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".lists.ListsActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".addeditlist.AddEditListActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>